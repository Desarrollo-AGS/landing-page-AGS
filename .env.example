# Envío del formulario de contacto (F-11). Sin estas dos variables la ruta
# /api/contacto responde 503 y el formulario ofrece el correo directo como
# alternativa, en vez de fingir que envió.
RESEND_API_KEY=
CONTACTO_REMITENTE="Web AGS <web@agssoluciones.cl>"
